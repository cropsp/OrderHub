# Environment
.env

# MCP client registration — the prod server entry carries the agent password inline
# (per-server env overrides mcp_server/.env), so this file is a secret. Never commit.
.mcp.json

# Env/secret backups (never commit)
*.bak
*.bak.*
*.env.bak*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node
node_modules/
dist/
.vite/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Uploads (Docker volume)
backend/uploads/
orderhub_data/
backend/logs/

# Logs
*.log
*.db
*.sqlite3
scratch/

# Internal audit / historical docs (kept locally, not pushed)
docs/integrations/nova-poshta-audit-2026-05.md
task.md

# Local-only dev startup wrapper (AI_ONBOARDING §9) — every machine's copy differs.
start-dev.sh

# Financial reports — real revenue, fees and payout figures. Local only, never pushed.
docs/finance/

# Per-session agent working artifacts (transient, regenerated each review)
COWORK_HANDOFF.md
REVIEW_FINDINGS.md
VISUAL_REVIEW.md
contrast-check.md
review-screenshots/
visual-review-screenshots/
