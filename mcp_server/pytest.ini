[pytest]
# Modules live flat in mcp_server/ (main.py, client.py, …) because the stdio
# entry point is invoked as a plain script path by the MCP client. `pythonpath`
# puts them on sys.path so tests can import them without a package shim.
pythonpath = .
testpaths = tests
asyncio_mode = strict
