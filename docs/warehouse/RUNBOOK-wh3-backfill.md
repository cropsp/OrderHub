# WH-3 — операційний бекфіл складу (runbook)

**Дата:** 2026-08-08
**Статус:** готовий до виконання після мержу+деплою WH-4.
**Це не кодовий спринт.** Виконавці: бекфіл-агент (окремий чат з MCP-доступом до OrderHub) + Сергій (інвентаризація, апруви). Cowork верифікує.
**Вхідні дані:** `docs/warehouse/upakovochka-purchase-history.md` (Секція A — 101 рядок замовлень, Секція B — 53 унікальні позиції).
**Рішення Сергія (2026-08-08, зафіксовані в DESIGN §9):** заводяться всі 53 позиції; `max_weight_g` = 5000 г дефолт; в історичні витрати включаються всі замовлення, крім дубля **#250318** і непідтвердженого **#392122**; історичні витрати — **unallocated** (NULL-shop, рішення DESIGN §10.2); інвентаризація — після запуску (§10.3); цінова база — остання закупівельна ціна (§10.6).

---

## Передумови (перевірити перед стартом)

1. WH-4 змержений і локальний MCP-процес бачить три нові тули (`list_packaging`, `create_packaging_box`, `update_packaging_box`).
2. Агентський користувач має всі магазини + `view_costs` (перевірено при деплої WH-4; див. AGENT-GRANT-REPLACE).
3. Бекфіл-агент має доступ до репо (Cowork з примонтованою папкою OrderHub) і читає вхідні дані сам: `docs/warehouse/upakovochka-purchase-history.md`.
4. MCP націлений на **прод**: в клієнті підключити сервер `orderhub-prod` (в ідеалі — лише його на цю сесію; `.mcp.json` реєструє dev і prod окремо). Промпт додатково змушує агента звірити відбиток через `list_shops` (прод = Lamamarka ETSY / Lamamarka Shopify / Vine&Roses ETSY) перед будь-яким записом. Захист від помилок: двофазний режим (план → апрув Сергія → виконання) — бо overhead-витрати не мають delete в UI (`OVERHEAD-RECEIPT-DELETE`), а архівування матеріалу незворотне (`MATERIAL-UNARCHIVE`). Агент **нічого не архівує** ні за яких умов.

## Фази

**Фаза 1 — каталог (агент). ✅ DONE 2026-08-08.** 49 позицій `suggested_target=packaging_boxes` через `create_packaging_box`; 4 позиції `suggested_target=materials` (картонний лист, гофрокартон, картон крейдований, деревна стружка) через `create_material`; одна overhead-позиція «Пакування — історичні закупки (Упаковочка)» через `create_overhead_material`. Правила мапінгу — в промпті нижче.

**Фаза 2 — історичні витрати (агент). ✅ DONE 2026-08-08.** 33 замовлення Секції A (35 мінус #250318, #392122) → по одному `record_overhead_expense` на замовлення: `total_cost` = сума рядків замовлення, `received_at` = дата замовлення, `invoice_no` = номер, `supplier` = «Упаковочка», без `shop_id` (unallocated). Рядки всіх 53 позицій включаються (і ті, що пішли в materials, — ретроспективного споживання для них теж не буде).

> **РЕВІЗІЯ ФАЗ 3+ (2026-08-08, підтверджено Сергієм):** до пайплайну додано **ретроспективне списання** по ~півроку замовлень 2026-го, що вже в базі — разовий прогін ДО перших партнерських settlement-ів (жодного розрахованого settlement-а ще не існує, тож незмінність історії не порушується; це реалізація припаркованого `historical-COGS-recompute`). Порядок фаз нижче — оновлений. **Жорсткий дедлайн: усі фази — до першого settlement-а.** Потрібен спринт **WH-5**: поле `default_packaging_box_id` на продукті + retro-runner з dry-run.

**Фаза 3 — BOM-и + дефолтні коробки. ✅ DONE 2026-08-14 (частково — див. «Хвости Фази 3»).** Рецепти (BOM) через `set_product_bom`/`add_bom_line`, дефолтна коробка через `set_default_packaging` (WH-5). Критично: замовлення БЕЗ BOM не отримає вартості в ретро-прогоні (правило WH-2 ① — box-only cost свідомо не бронюється), тож покриття BOM = покриття COGS.

Виконано: **4 сервісні матеріали** (пошиття) + **18 BOM / 109 ліній** + **21 дефолтна коробка**. Покриття BOM 1/59 → **19/59**, коробок 0/59 → **21/59**, матеріалів 27 → 31. Пропущено 0, відмовлено 0, дедуп-guard не спрацював жодного разу. Протокол з усіма числами, деривацією і рішеннями Сергія — `docs/warehouse/bom-source-data.md`.

Контроль: `compute_product_cost` збігся з прев'ю протоколу на всіх 11 групах рецептів (Δ ≤ 0.01 — округлення на рівні колонок проти акумуляції в тулі). Покриття «до» **виміряне**, а не взяте з протоколу: прогін `compute_product_cost` по всіх 40 продуктах поза скоупом повернув порожній basis. BOM `LM-CARD-BAT` (пілот) не чіпали — тільки коробка.

**Фаза 4 — інвентаризація + стартові залишки (Сергій → агент).** Фізичний перерахунок КОРОБОК і BOM-МАТЕРІАЛІВ (шкіра тощо — їм теж потрібен реальний WAC, інакше ретро-прогін спише матеріали по нулю). Дата перерахунку = дата зрізу.

Агент: для кожної позиції з ненульовим фактом — `record_material_receipt` проти `material_id` (для коробок — з `list_packaging`), `qty` = факт, `unit_cost` = остання закупівельна ціна (для коробок — `last_unit_price` Секції B), `received_at` = дата зрізу, `notes` = «Інвентаризація — стартовий залишок». Оскільки сток до цього 0, WAC = остання ціна — рівно рішення §10.6.
**УВАГА: саме receipt, НЕ `adjust_material_stock`** — adjustment не встановлює собівартість, WAC лишиться 0.0000, і всі списання підуть по нулю. (Фінальний звіт Фаз 1-2 помилково згадував adjust — ця примітка його виправляє.)

**Фаза 5 — ретро-прогін (WH-5 runner; тільки після Фаз 3-4).** Передумова: фліп `is_stock_tracked=false` на порізці/пошитті (`SVC-MATERIAL-NONSTOCK-OP`). Порядок: dry-run звіт (замовлення → визначена коробка → вартість; окремо списки «без BOM» і «без коробки») → апрув Сергія → виконання. Прогін списує і BOM-матеріали, і коробку кожного замовлення; COGS лягає на замовлення, тож у фінзвітах вартість опиниться в правильних місяцях автоматично (агрегати бакетують по датах замовлень, дата руху неважлива). Лічильники тимчасово підуть нижче факту або в мінус — очікувано, виправляє Фаза 6.

**Фаза 6 — вирівнювання + корекція подвійного обліку (§10.2, розширена формула).** (а) ADJUSTMENT кожній позиції до фізичного факту: delta = факт − поточний лічильник (adjustment не змінює WAC — тут він доречний). (б) Корекційний overhead-запис датою зрізу: **мінус (вартість ретро-спожитого пакування + вартість залишку на руках)** — обидва числа відомі після прогону; це гасить подвійний облік з історичними витратами 2020–2025, бо і спожите у 2026-му, і залишок куплені в межах заведеної історії. Якщо валідація відхилить від'ємний `total_cost` — агент зупиняється і повідомляє суму; рішення окремо.

**Фаза 7 — верифікація + closure.** Агент: підсумкова `list_packaging` зі звіркою «залишок == факт»; вибірка замовлень з новою вартістю (по одному на магазин); звірка Σ Фази 2 == 24 540.90 незмінна. Cowork: UI-перевірка (packaging, materials, overhead, дашборд; вибіркові замовлення з COGS) + closure entries WH-3 і WH-5 в `implementation_plan.md`. Після цього епік закритий — можна рахувати партнерів.

**Статус виконання (2026-08-08):**
- ✅ **Фази 1-2 — DONE на прод** (87 записів, контрольна сума 24 540.90 UAH до копійки, overhead_id `6b071b41-bfad-42b7-9c89-9ee68f488639`).
- ✅ **Хвости**: лічильники файлу-джерела виправлені (23/49 unknown, 26 підтверджених, 20 з тарою, 29 з артикулом — арифметика збігається); `max_weight_g=15000` двом гігантам (580×480×130 `ccccfa27…`, 600×400×400 `de3437e3…`), решта 47 на 5000.
- ✅ **Конверти Бориспіль перенесені з overhead у пакування**: три ENVELOPE створені (№11 `0c17013c…`, №12 `a50e5f43…`, №13 `7a52d1c2…`; 30 мм номінальна висота), три overhead-позиції заархівовані через UI (Cowork-субагент; історичні витрати 2/2/1 записів лишились на архівних позиціях). Каталог тепер: **52 позиції, з них 4 ENVELOPE**.
- 📌 **Ціни для Фази 4 (останні закупки, для стартових receipts конвертів)**: №11 100x160 — **2.34** UAH/шт; №12 120x210 — **3.00**; №13 150x210 — **3.60** (куплений раз, 2026-05-06). Всього за історію: №11 300 шт/702.00, №12 300 шт/900.00, №13 50 шт/180.00.
- ✅ **Фаза 3 — DONE на прод 2026-08-14** (18 BOM / 109 ліній, 21 коробка, 4 сервісні матеріали). Покриття 19/59 і 21/59.
- ⏳ Фази 4-7 — попереду.

---

## Хвости Фази 3 (відкриті на 2026-08-14)

### БЛОКЕР Фази 5 — порядок операцій

**Ставки пошиття мають бути проставлені ДО ретро-прогону.** Чотири сервісні позиції створені з `0.0000`; знімок собівартості замерзає на замовленні назавжди (`cogs_basis_amount` + rate), тож прогін на нульових ставках недооцінить ВСЮ історію на вартість пошиття, і підняти це назад уже не вийде. Це не косметика — це та сама односторонність, що й порядок Фаз 4→5.

**Той самий прохід по UI зручно поєднати з фліпом `is_stock_tracked=false`** (передумова `SVC-MATERIAL-NONSTOCK-OP`) — 6 позицій, з них 4 нові вийшли з `true`:

| Матеріал | material_id | Дія |
|---|---|---|
| Пошиття карабін | `f23ce3d0-391f-4e4d-98a2-93649428624f` | ставка + фліп |
| Пошиття магніт | `bb8e9dae-b534-4699-86e2-c5e81292ca14` | ставка + фліп |
| Пошиття гаманець SET-MAGNET | `10c3fbdd-dd3e-4ec1-81f3-c87bb9d8eda2` | ставка + фліп |
| Пошиття гаманець SET-BOLD | `f1d9cb3c-9c96-4e49-a706-452d3e44312c` | ставка + фліп |
| Лазерна порізка | `84fc3c30-a164-49fd-bc42-f12230ee870c` | лише фліп (ставка 16.00 є) |
| Пошиття Bat ID Wallet | `bc80be9c-c1a5-455d-a1b9-722d872f9c6b` | лише фліп (ставка 60.00 є) |

> Ставка задається **receipt-ом** (`record_material_receipt qty 1, unit_cost <rate>`), не редагуванням поля — `current_unit_cost` рухається лише через receipts (`bom-intake.md`). Через UI це «надходження матеріалу».
>
> **Порядок безпечний у будь-якому напрямі.** Прапорець блокує лише списання при відвантаженні, не надходження — підпис у формі: «the cost still lands on every order that uses it, but *shipping* never decrements a quantity», підтверджено в коді (`MaterialDetailPage.tsx:98-100`: «Receipts and adjustments stay available: an untracked service material still needs a receipt to carry its unit cost»).

#### 🐞 БЛОКЕР — прапорець не зберігається через UI (2026-08-15)

Спроба виконати `SVC-MATERIAL-NONSTOCK-OP` через браузер **провалилась**: галочка «Does not consume stock» на `Пошиття карабін` знімається, модалка закривається без помилки, значення не зберігається. Три спроби, з повним перезавантаженням між ними.

Кожна ланка окремо справна і має зелений тест:

| Ланка | Стан | Доказ |
|---|---|---|
| Схема PATCH | ✅ | `schemas/material.py:58` — `is_stock_tracked: Optional[bool]` у `MaterialUpdate` |
| Хендлер | ✅ | `routers/materials.py:131,159-160` — `model_dump(exclude_unset=True)` + `setattr` |
| Тест бекенду | ✅ | `test_materials_router.py:446-452` |
| Payload модалки | ✅ | `MaterialFormModal.tsx:102` |
| Тест фронту | ✅ | `MaterialFormModal.test.tsx:81-87` |
| **Наскрізно** | ❌ | не зберігається |

**❌ Гіпотеза Cowork (скидання форми по референсу `initialData` + фоновий refetch) — СПРОСТОВАНА CC 2026-08-15.** Три незалежні докази:

1. `frontend/src/main.tsx:13` — `refetchOnWindowFocus: **false**`, `staleTime: 30_000`, і так з коміту `dc3e46b` (2026-04-19). Названий тригер ніколи не був увімкнений у цьому застосунку. Помилка Cowork: міркував з дефолтів React Query замість того, щоб прочитати конфіг.
2. `@tanstack/react-query ^5.99.2`, `structuralSharing` за замовчуванням `true` — `replaceEqualDeep` повертає **попередній об'єкт**, коли вміст незмінний. Ідентичність не змінилась би навіть при refetch-у.
3. **Лог доступу прода:** між кожним завантаженням сторінки і збереженням немає GET `/api/materials/f23ce3d0…`. Refetch-у не було взагалі. Більше того — на двох спробах з трьох **не було й PATCH**. Це не «поганий payload»: запит не відбувся.

**✅ Справжній механізм — недосяжний футер діалогу.** `components/ui/dialog.tsx:62`: `DialogContent` — `fixed top-1/2 -translate-y-1/2` **без `max-height` і без `overflow-y`**; `MaterialFormModal.tsx:144` додає `overflow-hidden`; Radix блокує скрол `body`. Edit-варіант рендерить картку «Stock policy», якої немає в create-варіанті (`:258-307`) — разом ~1050px. На 1440×900 діалог виступає симетрично, ~75px зрізано зверху і знизу, доскролити неможливо. Клік, націлений у футер, потрапляє в оверлей → `onPointerDownOutside` закриває діалог. Модалка закрилась, помилки немає, запиту немає — точно описаний симптом.

**«Переміжність» залежить від висоти в'юпорта, а не від таймінгу.** Агент на 1440×900 програє завжди; Сергій на вищому екрані зберігає нормально — саме це й сталося о 17:23:56, тому `Пошиття карабін` на проді зараз читається `is_stock_tracked: false`, а решта п'ять — ще `true`.

`ProductForm.tsx:135` цю проблему у своєму діалозі вже вирішує (`max-h-[60vh] overflow-y-auto`). Ця модалка такого не отримала.

**Скидання форми лишається реальним, але вузьким латентним дефектом** (`MaterialFormModal.tsx:64-81`, скидає всі 9 полів + `setError`). За `refetchOnWindowFocus: false` плюс структурне шарування дістати його можна лише одним шляхом: зробити receipt або adjustment на сторінці деталі й відкрити Edit до того, як розв'яжеться інвалідаційний refetch (`hooks/useMaterials.ts:113-116,143-145`). Виправляється в тому ж спринті.

**Спринт `MAT-UI-1` — ✅ змержено й задеплоєно 2026-08-17** (`303e50d → ffc9d8e`, fast-forward; closure entry в `implementation_plan.md`). Перевірено на проді: діалог 759×730 проти зламаних 1012×384, футер поза скрол-контейнером, смоук збереження пройдено в обидва боки. Follow-up-и, знайдені по ходу: той самий дефект скидання у `PackagingForm.tsx:52-53` (дослівна копія, ще й не скидає `error`); той самий необмежений `DialogContent` у **`MaterialReceiptModal`, `PackagingForm`, `OverheadMaterialFormModal`, `OverheadMaterialReceiptModal`** — усі експоновані на недосяжний футер; клас `custom-scrollbar` (`ProductForm.tsx:135`) використовується один раз і ніде не визначений.

> ⚠️ **Операційно:** `MaterialReceiptModal` — це те саме вікно, в якому задаються ставки пошиття. Після кожної ставки звіряй `AVG UNIT COST` на картці матеріалу; якщо там 0.00, збереження не відбулось.

Стан прода від спроб Cowork не змінений — жодна нічого не записала.

### Перевірити фізично

- **Коробка на сети.** `LM-SET-MAGNET` і `LM-SET-BOLD` — гаманець плюс магніт/карабін в одній позиції; протокол призначив їм конверт №12 (120×210×30) як усім. Якщо фізично не лізуть — перевести дефолт на коробку, один `set_default_packaging` на продукт.
- **Метраж нитки** — стартові оцінки за периметрами швів (карабін 1.0 м, магніт 1.8 м, гаманці 3.0 / 3.5 м). Уточнити заміром; вплив на собівартість ~0.6–2.8 грн/од, тобто некритичний, але дешево виправити.
- **Чорна шкіра `BLACKNOV`** — записано `Крейзі Хорс AN чорна 027515`. Сергій кроїть дрібні деталі і з `ВОРОТ чорна 026349` (@7.90). Різниця ≈8 грн/од, 7 продажів lifetime. Якщо воріт основний — поміняти `material_id` однією лінією; розбіжність по складу все одно знімає ADJUSTMENT Фази 6.

### Продукти без BOM — 40 з 59

Кожен з них у ретро-прогоні дасть **NULL cost** (правило WH-2 ①). Класифікація:

**(а) Покупний товар — потрібна закупівельна позиція (3):**

- `LM-SOCK-MUSH-4043-WHT` (25 шт lifetime) — ✅ **рішення ухвалене 2026-08-14.** Один матеріал `Шкарпетки ПІСЮН (покупні)`, unit `pcs`, `is_stock_tracked=true`, BOM-лінія 1 pcs, дефолтна коробка — конверт №12. Ціна **104.00 UAH/пара**: накладна №20075 від 2026-05-29, 40 шт, 6 400.00 мінус знижка 2 240.00 (35%) = 4 160.00. Стартовий залишок — receipt у Фазі 4 за фізичним перерахунком (до цієї накладної були ще закупки; 135 шт у Shopify — вигадана цифра, див. нижче).
- `LM-BOX-SECRET` (3), `LM-BOX-ROCKET` (2) — **рішення не ухвалене**, потрібна накладна або ціна закупки.

> **Лічильники Shopify не є даними.** Сергій виготовляє під замовлення, а Shopify вимагає ненульового залишку, щоб лістинг лишався купованим — тому кількості там проставлені довільно. Джерело правди по залишках — OrderHub. Це рішення 2026-08-14 і воно перевертає інтуїтивний висновок «Shopify уже веде поштучний облік, не дублюймо»: дублювати нема чого, там декорація.

**(б) Свідомо пропущені рішенням Сергія 2026-08-14 (3):**
`LM-CARAB-CIRCULAR` (5 шт — «просто кільце»), `LM-CLIP-BAT` (2 — форма кажана, свій крій), `LM-CLIP-AIR` (1). Разом 8 шт lifetime. Повернутись, якщо з'являться скріни.

**(в) Немає скріна розкладки — 13 продуктів, 35 шт lifetime:**
`LM-KEY-BUBU-DANGLE` (8), `LM-KEY-BAT` (6), `LM-AIRTAG-LUCKYPAIR-BROWN` (4), `LM-KEY-MUSHROOM` (3), `LM-BELT-FACE-XL-BLACK` (3), `LM-BIFOLD-CAVEART` (2), `LM-BELTPOUCH-BEETLE` (2), `LM-BAG-TATTOO` (2), `LM-KEY-BUBU-FULL` (1), `LM-BIFOLD-LEGENDARY` (1), `LM-KEY-BUBU-BALLERINA` (1), `LM-BELT-FACE-BR-XL-BROWN` (1), `LM-BAG-JESUS` (1).
Двом з них (`BELT-FACE-*`) потрібна ще й ремінна смуга — вона в каталозі є (`017962`, `032433`, обидві pcs). `LM-AIRTAG-*` потребує AirTag-фурнітури, якої в каталозі немає.

**(г) Весь Lamamarka ETSY — 21 продукт.** Скрінів розкладок немає жодного; більшість — ID Card Holder / Cardholder Wallet, яких серед Shopify-скрінів немає. Найбільша дірка з чотирьох: це цілий канал продажів, і всі його замовлення у вікні ретро-прогону забронюють нульовий COGS. Три позиції виглядають дублями Shopify і закриваються копіюванням: «Leather Belt Carabiner, Novelty Keychain», «Leather Money Clip: Mature Adult Humor», «Personalized Bat ID Card Holder (book)» ≈ `LM-CARD-BAT`. `Delivery` — SKIP (службовий лістинг).

**Рекомендований порядок закриття:** (г) ETSY-дублі → (г) решта ETSY після скрінів → (в) за спаданням продажів → (а) рішення по дерев'яних коробках → (б) за наявності скрінів.

**Люверси в брелках** не заведені — окрема розмова, свідоме рішення 2026-08-14.

### Обмеження моделі: BOM на продукт, а не на варіант

**Знайдено 2026-08-14 під час Фази 3. Це межа моделі, не помилка бекфілу** — `bom-intake.md` її прямо називає: «Colour variants that would differ only in the leather material therefore need a product/variant split to carry different recipes».

Кольорові варіанти в Shopify — це різна шкіра, але BOM чіпляється до продукту, тож один рецепт застосовується до всіх варіантів:

| Продукт | Варіантів у Shopify | Кольори | Продано |
|---|---|---|---|
| `LM-CLIP-NOVELTY` | 5 | Brown, Black, Red, Pink, Dark Brown | 204 |
| `LM-KEY-BOLD-PINK` | 3 | Pink, Brown, Black | 100 |
| `LM-CLIP-TRIO-BRN` | 6 | Brown, Ruler, Black, Red, Pink (+1) | 18 |
| `LM-CLIP-XL-BRN` | 2 | | 6 |
| `LM-SOCK-MUSH` | 8 | 4 кольори × 2 розміри | 25 |

**Наслідок:** відвантаження чорного money clip списує камель + рожеву; чорна шкіра не зменшується ніколи. У грошах дрібниця (шкіри 7.90–16.13 UAH/dm², розкид кілька гривень на виріб), у **залишках** — систематичний дрейф на ~353 одиницях lifetime. Тобто ламається рівно та функція, заради якої склад і будувався.

**Рішення Сергія 2026-08-14: приймаємо як є, виправляємо окремим спринтом.** Обґрунтування: (а) поточний стан усе одно на порядок кращий за відсутність BOM; (б) Фаза 6 вирівняє лічильники ADJUSTMENT-ом на дату зрізу, тож стартова точка буде чиста, а дрейф після неї — повільний і видимий; (в) чекати кодовий спринт до ретро-прогону не можна, дедлайн — перший партнерський settlement.

**→ Бэклог: BOM на рівні варіанта.** Відхилені альтернативи: розбити продукти в CRM на окремі під колір (воює з Shopify-синком — наступний синк переб'є); відкласти Фазу 5 до спринту (ламає дедлайн).

### Розходження варіантів CRM ↔ Shopify — ✅ діагноз є (CC, 2026-08-15)

| Продукт | У CRM | У Shopify | Бракує |
|---|---|---|---|
| `LM-CLIP-NOVELTY` | 4 | 5 | `-DKBRN` |
| `LM-CLIP-TRIO-BRN` | 3 | 6 | `-RULER`, `-BLK`, +1 |
| `LM-SOCK-MUSH` | 6 | 8 | `4043-CRM`, `4447-GRY` |

**Це не баг синку — синку каталогу не існує і ніколи не існувало.** Каталог CRM є побічним продуктом синку замовлень: у `backend/services/shopify_sync.py` є `ORDERS_QUERY`, `PRODUCT_IMAGE_QUERY`, `REFUNDS_QUERY` — і жодного запиту `products` / `productVariants`. Варіант народжується лазі при першому замовленні з цим SKU (`catalog_import.py:72-77` «Lazily create»; `ensure_catalog_row:773`). Гейт «замовлення вже є → skip» (`shopify_sync.py:1013-1022`) не викликає `_write_order_node`, тож повторний синк каталог не чіпає — це задокументована ідемпотентність, є тест (`test_shopify_sync_catalog.py:252-290`, `assert result.variants_created == 0`).

Підтверджено даними: `created_at` варіантів — це моменти перших продажів, а не імпорту (21 шт на 2026-07-17 при імпорті історії, далі крапельно; останній `LM-CLIP-XL-RED` 2026-08-01). **Отже відсутні SKU — це варіанти, яких жодного разу не купили.** Жоден із них не осів на чужому продукті.

Зворотного напряму теж немає: знятий із Shopify варіант у CRM не архівується, `is_active` варіанта не пише жоден шлях синку.

**Прив'язка до чужого варіанта структурно неможлива:** в `order_items` немає колонки `product_id` (`models/order.py:322`) — лише nullable `product_variant_id` (ON DELETE SET NULL) і `sku` як текстовий знімок без FK. Ні fuzzy-матчингу, ні fallback-у на перший варіант — усі запити каталогу це точна рівність (`catalog_service.py:271-275, :281`). Зіпсованих даних у замовленнях немає.

### ✅ ГЕЙТ перед Фазою 5 — ВІДКРИТИЙ (виміряно CC, 2026-08-15)

**17 неприв'язаних `order_items` із 796 (2.14%)**, у 17 замовленнях, 2026-01-08 … 2026-08-14. Замовлень без жодного рядка — **0** (дефект вебхука на проді не матеріалізувався).

| Магазин | unlinked | замовлень | період | з них sku IS NULL |
|---|---|---|---|---|
| Lamamarka Shopify | 11 | 11 | 2026-01-08 … 08-14 | 6 |
| Lamamarka ETSY | 6 | 6 | 2026-02-11 … 07-09 | 0 |

**Вердикт: жоден неприв'язаний рядок не потрапляє на відвантажене замовлення продукту з BOM.** Ретро-прогін нічого не пропустить.

Це три різні дірки, не одна:

① **5 рядків (6 одиниць) — дефект наживо, на продуктах із BOM.** Замовлення `91890_1867`, `_1868`, `_1882`, `_1884` (qty 2) з `LM-CLIP-XL-BRN` і `_1881` з `LM-CLIP-XL-PNK`, 3–14 серпня. Усі в статусі **NEW**, тож у ретро-прогін (він бере відвантажені) не входять — але **відвантажаться без списання BOM**, якщо не втрутитись.

② **6 рядків — той самий дефект, інший тригер, Lamamarka ETSY.** SKU `bat id holder book`: у замовленнях `listing_id = 4446986575`, а CRM-продукт `43113b46…` має `external_ref = 4347345198` — лістинг перевиставлено під новим ID зі старим SKU. ETSY має 0 продуктів із BOM, тож зараз без наслідків; вкусить у наступній ітерації.

③ **6 рядків — `sku IS NULL`, не лікується.** Spidey Multicard ×2, Spidey Web ID, Squid ID, Bat Card Wallet, «Reshipping fee — order 91890_1769» — кастомні рядки й видалені лістинги (`shopify_sync.py:711-712`, немає `variant.product`). Жоден не відповідає продукту з BOM.

> **Поправка до доступу:** роль у БД — `crm`, база — `crm_db`. Робоча команда: `ssh orderhub 'docker exec orderhub-postgres-1 psql -U crm -d crm_db -c "…"'`. Чернетка нижче містила `-u postgres -d orderhub` — невірно.

<details><summary>Запит для повторного заміру</summary>

`order_consumption_service.py:205-207`: `if item.variant is None … continue` → **BOM не рахується, матеріали не списуються**. Кожен такий рядок — дірка в ретро-прогоні. Розмір дірки невідомий; порахувати ДО прогону:

```bash
ssh orderhub 'docker exec -u postgres orderhub-postgres-1 psql -d orderhub -P pager=off -c "
SELECT o.shop_id, COUNT(*) AS unlinked_items, COUNT(DISTINCT o.id) AS orders,
       MIN(o.ordered_at)::date AS first_order, MAX(o.ordered_at)::date AS last_order,
       COUNT(*) FILTER (WHERE oi.sku IS NULL) AS also_no_sku
FROM order_items oi JOIN orders o ON o.id = oi.order_id
WHERE oi.product_variant_id IS NULL
GROUP BY o.shop_id ORDER BY unlinked_items DESC;"'
```

Item лишається неприв'язаним у трьох випадках: (а) немає `variant.product` — кастомний рядок або видалений продукт (`shopify_sync.py:711-712`, видно в `items_without_sku`); (б) SKU зайнятий іншим продуктом магазину (`catalog_import.py:98-99`) — **ні лога, ні лічильника**; (в) виняток (`shopify_sync.py:787-791`, тільки лог).

</details>

### ✅ `SVC-MATERIAL-NONSTOCK-OP` — ЗАКРИТО 2026-08-17

Усі **12** сервісних позицій мають `is_stock_tracked=false`; усі ставки проставлені. Виконано Cowork через UI, обхід недосяжного футера — `form.requestSubmit()`, кожен фліп верифіковано повторним відкриттям модалки. Передумова Фази 5 знята.

Заразом **Фаза 4 частково закрита**: фанера отримала справжній стартовий залишок 591.7 dm² × 3.4984 (10 аркушів), а не токен-receipt.

### 🚧 ГЕЙТ — вікно нульової собівартості

Після заведення BOM шкарпеток `bom_equipped = 1`, а матеріал і конверт коштують 0.0000. **Будь-яке відвантаження шкарпеток до Фази 4 запише `computed_production_cost = 0.00`** — і цей нуль виграє `COALESCE(computed, manual, 0)` у finance COGS, PROFIT-базі партнерів і дашборді, перекривши навіть ручний `production_cost`. Знімок замерзає назавжди.

Те саме м'якше стосується решти 18 продуктів: ставки пошиття 0.00, тож кожне відвантаження до їх заповнення недооцінює собівартість на вартість пошиття.

Залишок піде в мінус — це нормально: receipt Фази 4 при `stock_quantity <= 0` перебазовує WAC, а не усереднює з від'ємним (WH-2), тож 104.00 стане чисто; лічильник вирівняє фізичний перерахунок.

### Два дефекти на окремий спринт (знайдені CC 2026-08-15, поза скоупом WH-3)

1. **Тихе несходження SKU — `catalog_import.py:98-99`.** Формулювання виправлено 2026-08-15: раніше дефект був описаний вузько, як «продукт без `external_ref`». Насправді вхід ширший — **достатньо будь-якої ситуації, де SKU в магазині належить не тому продукту, який приїхав у замовленні.** Тоді `find_product_by_external_ref` знаходить продукт замовлення, серед його варіантів SKU немає, `is_sku_taken` каже «зайнятий іншим» → `return None`. Варіант не лінкується, BOM не рахується, **сліду в логах немає**.

   Три відомі входи, з них два вже спрацювали на проді:

   | Вхід | Стан | Слідів |
   |---|---|---|
   | **Один SKU на двох Shopify-лістингах** | ✅ сталося, 5 рядків | 6 одиниць на продуктах із BOM |
   | **Перевиставлений Etsy-лістинг зі старим SKU** | ✅ сталося, 6 рядків | ETSY без BOM, поки без наслідків |
   | Ручний / CSV-продукт без `external_ref` | ❌ не сталося | продуктів без `external_ref` на проді **0** |

   > **Корекція попереднього запису:** тут раніше стояло «у Сергія є CSV-імпорт каталогу, тож ризик не теоретичний». Це було невірно — запит `WHERE p.external_ref IS NULL` дав 0 рядків, ручного вводу й CSV-імпорту в цій базі не було, увесь каталог народжений лазі-вставкою із замовлень. Дефект спрацював, але іншим шляхом.

   **Першопричина випадку ① (встановлено 2026-08-15 через Shopify API):** лістинг `XL Cheeky Leather Money Clip` (`9168181002396`, 49.99) продубльовано 2026-08-01 → оригінал переведено в **DRAFT**, копія `Yes, It's The One From The Video` (`9185204600988`, 59.99) стала ACTIVE **з тими самими п'ятьма SKU** `LM-CLIP-XL-{BRN,BLK,RED,PNK,DKBRN}`. CRM зареєструвала SKU за першим продуктом; замовлення тепер приходять з другого. Тобто тригер — **звичайне дублювання лістингу для тесту заголовка/ціни**, а не екзотика. Будь-який наступний дубль зробить те саме.
2. **Вебхук створює замовлення без line items** (`webhooks.py:127-131`), і полінг це вже не виправляє. Зараз неактивний (`has_shopify_webhook_secret: false`), тож не горить — але вмикати вебхуки до фіксу не можна.

### Застереження до цифр покриття

Знаменник «59» — це те, що знає CRM, а CRM знає лише **продане**. Нові лістинги без жодного продажу в нього не входять. Тобто «20/59» і «22/59» описують покриття історії замовлень (що для ретро-прогону і потрібно), але **виглядають повнішими, ніж покриття реального асортименту Shopify**.

## Відомі граблі (агенту про них сказано в промпті)

- **Дедуп-guards** відмовляють по `supplier_sku`, потім по імені — повторний прогін безпечний. Якщо guard відмовив по позиції, яка ВЖЕ існує в CRM (напр. коробка `100x120x50`, якщо вона є в історії) — правильна дія: `update_packaging_box` на існуючій (доставити артикул/tare), НЕ `allow_duplicate_*`.
- **ENVELOPE**: `inner_height_mm > 0` обов'язкове — ставити номінальну робочу товщину (30 мм) з нотаткою; `max_thickness_mm` — якщо невідомо, NULL.
- **`weight_g` Секції B — це tare** (вага порожньої коробки), НЕ max_weight. `max_weight_g` = 5000 всюди. `tare_weight_g` = weight_g, де відомо; інакше 0.
- Розміри — внутрішні мм; для 22 позицій `dims_are=unknown` — заводити як є, в notes матеріалу: «розміри з назви, не підтверджені карткою».
- Імена — дослівно з Секції B (кирилиця/лат «х» вже злиті там), ≤100 символів перевірено.
- Валюта скрізь UAH.

---

## Промпт для бекфіл-агента (скопіювати в чат з MCP-доступом до OrderHub і примонтованою папкою репо)

```
Ти — бекфіл-агент складу OrderHub. Працюєш через MCP-тули OrderHub (materials +
packaging) сервера **orderhub-prod** — і ТІЛЬКИ його. Якщо в сесії видно тули двох
серверів (orderhub-dev і orderhub-prod) — використовуй виключно префікс
mcp__orderhub-prod__*. ПЕРША ДІЯ, до будь-якого плану: виклич list_shops цього
сервера і звір відбиток — прод повертає магазини "Lamamarka ETSY",
"Lamamarka Shopify", "Vine&Roses ETSY". Якщо бачиш "KoraKlenu" / "LeatherCraft UA" /
"Leather by Mykola" — це DEV: зупинись і повідом мені, нічого не пиши.

Далі прочитай вхідні дані з репо:
docs/warehouse/upakovochka-purchase-history.md — історія закупівель «Упаковочка»
(Секція A — рядки замовлень, Секція B — зведений каталог 53 позицій). Прочитай
файл ПОВНІСТЮ (обидві секції, включно з розділами «Проблеми і прогалини» та
«Готовність до імпорту»). Твоє завдання — Фази 1-2:
завести каталог і історичні витрати. НЕ чіпай залишки (інвентаризація буде окремо),
НІЧОГО не архівуй, не створюй receipts на матеріали.

РЕЖИМ: спочатку ПЛАН, потім виконання. Крок 1: побудуй і покажи мені три таблиці —
(а) 49 коробок з полями для create_packaging_box; (б) 4 матеріали для create_material
(картонний лист, гофрокартон, картон крейдований, деревна стружка); (в) 33 overhead-
записи (замовлення мінус #250318 і #392122) з сумами і датами + контрольна сума.
Чекай мого «виконуй». Крок 2: виконуй по черзі, звітуй кожні 10 позицій, збирай
відмови дедупу окремо. Крок 3: фінальний звіт — створено/пропущено/відмовлено,
звірка контрольної суми витрат, повна таблиця list_packaging.

ПРАВИЛА МАПІНГУ (Секція B → create_packaging_box):
- name = item_name дослівно; packaging_type = BOX (ENVELOPE лише якщо позиція конверт:
  тоді inner_height_mm = 30 з нотаткою, max_thickness_mm = NULL якщо невідомо)
- inner_*_mm = L/W/H_mm; max_weight_g = 5000 (усім); tare_weight_g = weight_g якщо
  число, інакше 0
- supplier_sku = article (де є, 31 позиція); supplier_name = "Упаковочка (upack.kiev.ua)"
- notes: для dims_are=unknown — "розміри з назви товару, карткою не підтверджені";
  для позначених у файлі особливих випадків — перенеси нотатку з файлу
- low_stock_threshold не ставити (дефолт)
- 4 позиції suggested_target=materials: create_material, unit = аркуш/шт за змістом,
  currency UAH, supplier_sku/supplier_name так само, category MATERIAL

ПРАВИЛА ВИТРАТ (Секція A → record_overhead_expense):
- Спершу create_overhead_material "Пакування — історичні закупки (Упаковочка)", unit "грн"
- Один запис на ЗАМОВЛЕННЯ: total_cost = сума line_total рядків замовлення (включно з
  рядками 4 матеріалів), currency UAH, received_at = дата замовлення,
  invoice_no = номер замовлення, supplier = "Упаковочка", БЕЗ shop_id (unallocated)
- Виключи: #250318 (дубль #250317) і #392122 (непідтверджене)
- #133141: у нотатку запису додай "у джерелі згадка про повернення 348 грн по #130095"

ДЕДУП: guards відмовлять по supplier_sku або імені. Якщо позиція вже існує в CRM —
НЕ став allow_duplicate_*; замість цього update_packaging_box на існуючій (доставити
supplier_sku/tare/notes) і зазнач це у звіті. allow_duplicate_* — лише якщо я явно
підтверджу.

Якщо тул повертає PARTIAL — виконай указаний repair call одразу і зазнач у звіті.
Якщо щось незрозуміло в даних — питай, не вгадуй.
```

---

## Бутстрап-промпт для Фази 3 (новий Cowork-чат; продовження — Фази 4-6 тим самим чатом)

Передумови в новому чаті: Cowork-режим, примонтована папка `\\wsl.localhost\ubuntu\home\serhii\projects\OrderHub`, підключений MCP `orderhub-prod` (клієнт перезапущений після WH-5 — тул `set_default_packaging` має бути видимий).

```
Привіт! Я працюю над OrderHub CRM. Ти — операційний агент Фази 3 warehouse-епіку
(BOM-и + дефолтні коробки). Перевір, що у <env> примонтована папка OrderHub.

Прочитай у цьому порядку:
  1. docs/AI_ONBOARDING.md — workflow і path mapping (Bash по UNC не працює!)
  2. CLAUDE.md — правила проєкту; особливо Gotchas "Warehouse unification" і
     "Packaging consumed at SHIPPED"
  3. docs/warehouse/DESIGN-inventory-architecture.md §9 — ревізія порядку фаз
  4. docs/warehouse/RUNBOOK-wh3-backfill.md — ПОВНІСТЮ; ти виконуєш Фазу 3
  5. docs/warehouse/bom-intake.md — конвенція одиниць для BOM
  6. docs/warehouse/bom-source-data.md — список 59 продуктів двох магазинів з
     колонками для заповнення (правила дублікатів — у шапці файлу)
  7. docs/warehouse/bom-screens/ — скріни лазерних розкладок виробів; ім'я файлу =
     SKU з bom-source-data.md. На скріні видно ПЛОЩУ шкіри та ДОВЖИНУ ПОРІЗКИ —
     твоє завдання проаналізувати кожен скрін (Read читає зображення) і вивести
     з нього кількості: шкіра в одиницях за конвенцією bom-intake (з реальним
     кроєм), порізка — сервісна позиція за довжиною. Тип шкіри — з імені файлу
     або колонки «Нотатки»; якщо не вказаний — запитай. ФУРНІТУРА (карабіни,
     магніти, кнопки) на розкладках НЕ видна — збери її по кожному виробу
     списком питань до Сергія, не вигадуй

Правила роботи через MCP:
  - ТІЛЬКИ сервер orderhub-prod (mcp__orderhub-prod__*). Перша дія — list_shops
    і звірка відбитку: прод = "Lamamarka ETSY", "Lamamarka Shopify",
    "Vine&Roses ETSY". Побачив KoraKlenu/LeatherCraft — це dev, СТОП.
  - Двофазний режим для всіх записів: спершу повний план таблицями → мій апрув
    («виконуй») → виконання зі звітом кожні 10 позицій.
  - НІЧОГО не архівуй. Тули: set_product_bom / add_bom_line /
    set_default_packaging / list_products / get_product / get_product_bom /
    list_materials(category="MATERIAL") / list_packaging.
  - Дозволено оновлювати RUNBOOK-wh3-backfill.md (статус/галочки). НЕ чіпай
    implementation_plan.md і task.md.

Крок 1 (перед будь-чим): збудуй через MCP таблицю покриття по ВСІХ магазинах:
продукт → магазин → чи є BOM (скільки ліній) → чи є default_packaging_box_id.
Покажи мені — це базис для рішень, які продукти обробляємо.

Крок 2: проаналізуй скріни з bom-screens/ (по одному, звіряючи SKU з
bom-source-data.md) і збери план: per продукт — BOM-лінії (material_id,
qty_per_unit за конвенцією bom-intake; шкіра з площі розкладки, порізка з
довжини) + фурнітура (список питань до мене одним пакетом) + дефолтна коробка
(box_id з list_packaging; з колонки файлу). Заповни bom-source-data.md
результатами аналізу — він стає протоколом. Питання, а не здогадки. Продукти
без скріна і без даних — у список «непокриті», не блокують решту.

Крок 3: після апруву — виконання, фінальний звіт (створено/оновлено/пропущено,
покриття до/після), оновлення статусу Фази 3 в runbook.
```

## Після завершення

- Cowork пише closure entry WH-3 в `implementation_plan.md` (епік закривається).
- Сергій: `SVC-MATERIAL-NONSTOCK-OP` (фліп прапорця на порізці/пошитті), якщо ще не зроблено.
- Далі за межами епіку: BOM-покриття продуктів + прив'язка коробок до товарів (Сергій згадував «підв'язати під кожен товар» — це `default_packaging` рішення, окрема розмова), і тоді — партнерські розрахунки на повній фінкартині.
