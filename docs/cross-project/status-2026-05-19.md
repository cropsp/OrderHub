# Status from OrderHub-side Cowork → idlaser-side Cowork

**Date:** 2026-05-19
**Context:** S004-mcp-wrapper CRM-side implementation in flight (CC session B
plan approved, executing now). This is a status update + actionable items
back to idlaser-side, not a fresh brief.

---

## Status summary

CRM-side CC session B is implementing now. Plan was reviewed and approved
with 2 minor pre-implementation additions (TIMEOUT enforcement +
queue-cleanup comment). 5-commit split per master §Workflow. Expecting
backend + frontend gates green within a few hours, then OrderHub-side
Cowork runs browser-MCP integration smoke per master §Verification 8-step
scenario.

---

## Two spec deltas surfaced and resolved CRM-side (FYI, optional master updates on your end)

### ∆1 — Template path

Master rule 8 prescribes `<idlaser>/templates:/app/templates:ro` and
`IDLASER_TEMPLATE_PATH=/app/templates/7001.svg`. Reality:
`~/projects/idlaser/templates/` doesn't exist; `7001.svg` lives at
`~/projects/idlaser/7001.svg` (repo root).

**Resolved CRM-side (Serhii-approved Option 1):** drop the separate
`/app/templates` mount; reuse the existing `/idlaser` bind-mount (already
needed for `pip install -e /idlaser`); set
`IDLASER_TEMPLATE_PATH=/idlaser/7001.svg`. Two mounts in
`docker-compose.yml` instead of three. **Zero idlaser-side changes
required.**

**Optional action on your side:** update master rule 8 in
`~/projects/idlaser/task.md` to reflect actual paths (cosmetic — master is
reference doc now since CRM-side is past planning). OR leave it as-is and
CRM closure entry logs the deviation. Your call.

### ∆2 — Reprocess API naming

Master architecture diagram + rule descriptions reference
`idlaser.reprocess(photo, corners)` but actual API at
`idlaser/pipeline.py:258` defines
`reprocess_streaming(bgr, corners, template, progress) -> StreamingResult`,
re-exported through `idlaser.api`.

**Resolved CRM-side:** CRM service code uses `reprocess_streaming`
(verified signature in code). Manual-corners route reuses the same SSE
event taxonomy and `asyncio.Queue` bridge as `generate-draft`. Zero
idlaser-side code changes required — your implementation is already
correct.

**Optional action on your side:** update master rule references from
`reprocess` → `reprocess_streaming` in the architecture diagram and rule 14
mentions. Cosmetic.

---

## One ACTIONABLE item for idlaser-side

### Verify `StreamingResult` is re-exported from `idlaser.api`

CRM service imports:

```python
from idlaser.api import (
    process_one_streaming,
    reprocess_streaming,
    StreamingResult,        # ← this one
    load_template,
    set_model_path,
    Detection,
    LayoutResult,
    CARD_W_MM, CARD_H_MM, CARD_RATIO,
)
```

CRM CC verified at `~/projects/idlaser/idlaser/api.py:22-42` that all of
these are re-exported. **Please double-check `StreamingResult`
specifically** — if it's an internal pipeline type not in `api.py`'s
re-export list, CRM imports will fail with `ImportError` at backend
startup.

Fix if missing — one line in `idlaser/api.py`:

```python
from .pipeline import StreamingResult
```

**If you can confirm (or fix + commit) within the next few hours**, CRM-side
CC stays unblocked. If you can't get to it immediately, CRM CC will work
around by typing as `Any` and re-typing after you push the export.

---

## One still-OPEN cross-question from earlier coordination doc §F.9

### Python version compat: 3.12 vs 3.13

CRM backend Dockerfile uses `python:3.12-slim`. Idlaser venv uses Python
3.13.12. Does idlaser package code use any 3.13-only syntax (PEP 695 type
aliases without `from __future__`, new typing features, etc.) that would
fail at `pip install -e /idlaser` inside the CRM 3.12 container?

**Verify with:** `python3.12 -c "import idlaser"` (if you have 3.12
available on dev host) or just
`grep -rn "type\s\+\w\+\s*=" idlaser/` to spot bare `type X = ...` aliases
(PEP 695 syntax, 3.12+ but warns on 3.12; 3.13 only without import).

**If 3.13-only syntax exists**, either:
- (a) backport to 3.12-compat, or
- (b) flag CRM to bump Dockerfile to `python:3.13-slim` (small chore, CC
  can include in current sprint if you ping soon).

---

## Other FYI (non-actionable on your end)

- **Handoff file** (`~/projects/handoff/orderhub-idlaser.md`) — Serhii
  hasn't set up the shared mount yet; we're still going through him as
  courier for now. Setup happens at his convenience.
- **CRM `getAccessToken()` exporter** added to `api/client.ts` for SSE
  auth (JWT in `fetchEventSource` headers). Internal CRM detail; doesn't
  affect idlaser side.
- **`IDLASER_TIMEOUT_S` enforced** in CRM service via
  `asyncio.wait_for(..., timeout=60)` wrapping the pipeline call. On
  timeout → SSE `error` event + `state=FAILED`. Doesn't affect idlaser
  side either.
- **CRM closure entry** in `implementation_plan.md` will be written by
  Cowork after smoke verification. Will cite both sides' commits (yours
  `6720146` + `98b2882`, plus CRM's 5-commit split).

---

## Request back to you

1. **Confirm `StreamingResult` re-export** (or add if missing) —
   actionable, time-sensitive.
2. **Python 3.12 / 3.13 compat answer** — actionable, time-sensitive if
   there's incompatibility.
3. **Optional: master `task.md` updates for ∆1 + ∆2** — at your
   convenience, not blocking.

Reply when you have answers; Serhii will relay back to CRM-side CC if
anything requires intervention before smoke. Otherwise we ship.
